# Refinement of Quotations & Admin Module

This plan addresses layout issues on the dashboard, improves the settings page, and enhances the quotation creation process with new features like client selection and tax toggles.

## User Review Required

> [!IMPORTANT]
> **Database Update**: I will add `agent_name` and `is_tax_enabled` columns to the `quotations` table.
> **Layout Changes**: The dashboard will be restored and its header rearranged for better space utilization.

## Proposed Changes

### 1. Database Layer
- [MODIFY] Add columns to `quotations` table:
    - `agent_name` (VARCHAR 255)
    - `is_tax_enabled` (TINYINT 1, default 1)

### 2. Admin Controller
- [MODIFY] `quotations_create`: Fetch all clients from `User` model to populate the selection dropdown.
- [MODIFY] `quotations_store`: Handle the new `agent_name` and `is_tax_enabled` fields.

### 3. Dashboard Layout
- [MODIFY] [dashboard.php](file:///c:/xampp/htdocs/dominican_travel/app/views/admin/dashboard.php): 
    - Fix missing `<div class="adm-ph">` and `<div class="adm-ph-left">` tags.
    - Reposition "Settings" and "New Quote" buttons to avoid excessive whitespace.

### 4. Quotation Creation Page
- [MODIFY] [create.php](file:///c:/xampp/htdocs/dominican_travel/app/views/admin/quotations/create.php):
    - **Client Dropdown**: Add a searchable `<select>` for existing clients.
    - **Auto-Fill**: Add JS to fill email/phone when a client is selected.
    - **Agent Field**: Add an input for "Agente que lo atiende".
    - **Tax Toggle**: Add a Switch/Toggle to enable/disable ITBIS.
    - **JS Refactor**: Update `calculateTotals()` to ignore tax if disabled.
    - **Layout Fix**: Fix the `textarea` for notes and its container to prevent footer overlap.

### 5. Settings Page Refinement
- [MODIFY] [settings.php](file:///c:/xampp/htdocs/dominican_travel/app/views/admin/settings.php):
    - Complete UI overhaul for a more professional, "Premium" look.
    - Improved spacing, icon usage, and visual consistency.

### 6. Printing & Signs
- [MODIFY] [print.php](file:///c:/xampp/htdocs/dominican_travel/app/views/admin/quotations/print.php):
    - Respect the `is_tax_enabled` field.
    - Add customization options (e.g., small/large font toggles).
- [MODIFY] [airport_sign.php](file:///c:/xampp/htdocs/dominican_travel/app/views/admin/quotations/airport_sign.php):
    - Ensure logo and name are optimally positioned for maximum visibility.

## Open Questions

1. **Client Selection**: Should we still allow manual entry of client data if they are not in the database? (I'll assume yes, as a fallback).
2. **Print Customization**: What specific "style/size" options would you like? (I'll start with Font Size and Theme Color toggles).

## Verification Plan
- **Dashboard**: Verify the layout is intact and the "blank space" is gone.
- **Quote Flow**: Create a quote selecting an existing client. Verify the agent name is saved.
- **Tax Toggle**: Create a quote with tax disabled. Verify the final total does not include it.
- **Settings**: Verify the new design looks professional and saves data correctly.
- **Print**: Check the printable version for both enabled/disabled tax.
